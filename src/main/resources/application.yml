spring:
    datasource:
        url: jdbc:postgresql://localhost:1543/clasnapp
        user: postgres
        password: password
    jpa:
        hibernate:
            show-sql: true
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect