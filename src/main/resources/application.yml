spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/clasnapp
        username: postgres
        password: password
    jpa:
        hibernate:
            ddl-auto: create-drop        
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: true